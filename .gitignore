# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# environment variables (APIキーなど。絶対にコミットしない)
.env
.env.local
.env*.local

# collected IR PDFs (ローカル保存物。念のため)
/collected

# misc
.DS_Store
*.log
